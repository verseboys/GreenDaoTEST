apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    compile group: 'de.greenrobot', name: 'greendao-generator', version: '2.0.0'

}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
